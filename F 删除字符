//F 删除字符
/*
题目描述 
输入一个字符串(不多于80个字符)和一个待删除字符，从该字符串中删除指定字符，输出删除后的字符串。 
输入 
输入数据有多组。每组数据有三行，第一行为数据组数，后续两组占两行，分别表示字符串和待删除字符。 
输出 
对于每组输入数据，输出一行，对应删除指定字符后的字符串。 
样例输入 Copy 
2
How are you!
!
Fine thank you
n
样例输出 Copy 
How are you
Fie thak you
*/

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
	int i,n,j,k;
	char str[80],ch;
	scanf("%d",&n);
	getchar(); //读走回车
	for(i=0;i<n;i++)
	{
		gets(str); //与scanf不同，gets可以自动换行（即自带回车）
		scanf("%c",&ch);
		getchar(); //读走回车
		j=0,k=0;
		while(str[k]!='\0')
		    if (str[k] != ch) 			
                str[j++] = str[k++];            
          else 			
                k++; 
      str[j] = '\0';
    printf("%s\n", str);   
	}
	return 0;
}
