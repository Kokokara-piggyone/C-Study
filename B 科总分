/*
题目描述 
输入10个学生的学号和5门课程的成绩，统计输出5门课总分最高和最低的学生的学号和他们的总分。
输入 
每行6个整数，共10行。
其中，每行第一个整数表示学生的学号，剩下的5个数表示他5门课程的成绩。
输出 
两行，每行两个整数。
其中第一行为总分最高的学生的学号，和他的总分，用空格隔开。
第二行为总分最低的学生的学号，和他的总分，用空格隔开。
*/

#include<stdio.h>

int main()
{
    int i,j,n,sum;
    int max,idx_max,min,idx_min;
    int a[10][6],b[10];
    //freopen("d:\\in.txt","r",stdin);
    for(i=0;i<10;i++)
        for(j=0;j<6;j++)
        	scanf("%d",&a[i][j]);    
//下边这个循环啊。。。你 de了好久的 bug都没 de出来！
//可以这么理解，要求十个人的总成绩，所以要重复十次计算总成绩的循环，就是外循环。
//每一次计算总成绩时，都是将自己的五科成绩加起来，怎么加？就再来一个循环实现累加。
//累加完之后，注意是累加完之后，即已经退出循环了，此时将值赋给数组b中的相应元素存储。
//然后就将 sum初始化，继续循环呗。     	
    for(i=0;i<10;i++)
    {    
        sum=0;
        //求每位同学的总成绩 
        for(j=1;j<6;j++) 
        	sum+=a[i][j];
        //将每位同学的总成绩求出后存入数组 b[i]中的相应元素中 
        b[i]=sum;
    }
    
    max=b[0],idx_max=0;
    min=b[0],idx_min=0;
    
    for(i=0;i<10;i++)
		if(b[i]>max)
			max=b[i],idx_max=i;
    
    for(i=0;i<10;i++)
        if(b[i]<min)
            min=b[i],idx_min=i;
            
    printf("%d %d\n",a[idx_max][0],max);
    printf("%d %d\n",a[idx_min][0],min);
    return 0;
}
