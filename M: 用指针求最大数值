//M: 用指针求最大数值
/*
题目描述 
编写一个函数，找出数组中存储的最大数值是第几个元素
此题用C语言完成时，必须使用指针方法处理，只提交头文件#include "stdio.h"和int maxint(double *arr, double *val)函数，系统将自动附加下面的main函数后运行，请复制下面的main函数用于调试函数。
int main(void)
{
    int bigIdx;
    double max[5];
    double theBig;    
    bigIdx = maxint(max,&theBig);
    printf("%d,%.3lf\n,", bigIdx, theBig);
    return 0;
}

输入 
5个浮点数到一个数组

输出 
数组中存储的最大数值是第几个元素及其值

样例输入 Copy 
1.001 1.002 3.001 3.002 3.003
样例输出 Copy 
5,3.003
*/

#include<stdio.h>

int maxint(double *arr, double *val)
{
	int i,idx=1;
	for(i=0;i<5;i++)
		scanf("%lf",&arr[i]);
	*val=arr[0];
	for(i=1;i<5;i++)
	{
		if(arr[i]>*val)
		{
			*val=arr[i];
			idx=i+1;	
		}
	} 
	return idx;	 
}
//也可以通过double t; t=arr[i],arr[i]=*val,*val=t;来实现。
//问题一：idx为什么要初始化（为1）
  回答一：如果不初始化并且很不巧输入的这个字符串第0个数是最大的，那么此时输出的idx的值就不确定了，
  	 因为自始至终没有进入for循环，那么就不能通过idx=i+1;为i赋值。
//问题二：直接return i;不可以么？
  回答二：不可以，i用作在遍历时计数，所以每次输出的值都是5（至于为什么不是4，当然是因为先执行i++,再判断i<5是否成立）




