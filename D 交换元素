/*
题目描述 

输入一个正整数n（1＜n≤10），再输入n个整数，将最小值与第一个数交换，最大值与最后一个数交换，然后输出交换后的n个数。
输入 
第一行输入一个整数n，第二行输入n个整数。
输出 
将最小值与第一个数交换，最大值与最后一个数交换，输出交换后数组的整数，每两个整数之间用空格分隔。
样例输入 Copy 
5
3 1 0 4 2
样例输出 Copy 
0 1 3 2 4
*/
//法一： 
#include<stdio.h>

int main()
{
	int i,n,a[10];
	int min,idx_min,max,idx_max,temp;
	
	scanf("%d",&n);
	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
	min=a[0],max=a[0];
	
	for(i=0;i<n;i++)
		if(a[i]<min)
			min=a[i],idx_min=i;
	temp=a[0];
	a[0]=a[idx_min];
	a[idx_min]=temp;
	
	for(i=0;i<n;i++)
		if(a[i]>max)
			max=a[i],idx_max=i;
	temp=a[n-1];
	a[n-1]=a[idx_max];
	a[idx_max]=temp;
	
	for(i=0;i<n;i++)
		printf("%d ",a[i]);
	return 0;	
} 

//仍然不知道为什么错误33%的法二
#include <stdio.h>
int main()
{
	int i,n,max,min,temp,a[10];
	scanf("%d",&n);
	max = min =0; 
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);  
		if(a[i]>a[max])
			max = i; 
		if(a[i]<a[min]) 
			min = i;
	} 
	temp = a[max];  
	a[max] = a[n-1];
	a[n-1] = temp;
	
	temp = a[min];
	a[min] = a[0];
	a[0] = temp;
	for(i=0;i<n;i++)
		printf("%d ",a[i]);  
	return 0;
} 
